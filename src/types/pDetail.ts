import { PlaceInfoType } from '@/types/placeInfo';

export type PDetailType = {
  name: string;
  category: string;
  place: string;
  duration: string;
  time: string;
  spectator: string;
  price: string;
  score: number;
  placeInfo: PlaceInfoType;
};
